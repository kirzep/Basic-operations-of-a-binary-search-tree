# Реализация Бинарного Дерева Поиска (BST) на Python

Этот проект представляет собой реализацию классического **Бинарного Дерева Поиска** (Binary Search Tree) с поддержкой типизации. Программа включает в себя основные операции для работы с деревом и набор автоматических тестов для проверки работоспособности.

## 📋 Описание функционала

Класс `BinarySearchTree` поддерживает следующие операции:

1.  **Вставка (`insert`)**: Добавляет пару "ключ-значение" в дерево. Если ключ уже существует, обновляет его значение.
2.  **Поиск (`search`)**: Ищет значение по ключу. Возвращает значение или `None`, если ключ не найден.
3.  **Удаление (`delete`)**: Удаляет узел по ключу, корректно перестраивая связи в дереве (обрабатывает случаи удаления листа, узла с одним или двумя детьми).
4.  **Высота (`height`)**: Возвращает максимальную глубину дерева (количество уровней).
5.  **Балансировка (`is_balanced`)**: Проверяет, является ли дерево сбалансированным (разница высот левого и правого поддерева не более 1).

## 🛠 Требования

* **Python 3.7+** (Необходимо для поддержки `from __future__ import annotations`).
* Никаких сторонних библиотек устанавливать не нужно, используется только стандартная библиотека.

## 🚀 Инструкция по запуску

Сохраните основной код программы в файл, например, **`bst.py`**.

### 1. Запуск на Windows (PowerShell или CMD)

Откройте терминал в папке с файлом и выполните:

```bash
python main.py